# Supabase (also hardcoded as fallback in src/utils/supabase.js
# because Cloudflare Pages env vars don't inject into the Vite bundle)
VITE_SUPABASE_URL=https://hosqexsglgfvhjgwdpau.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# EmailJS public key (also hardcoded as fallback in src/utils/email.js
# for the same Cloudflare Pages env var reason)
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here
